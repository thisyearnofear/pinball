# React app env (optional)
# Mezo Passport handles chain selection internally; these are for your own app logic later.
VITE_CHAIN_ID=31611
VITE_RPC_URL_PUBLIC=https://rpc.test.mezo.org

# Temporary: embed the legacy Vue game while we migrate
VITE_LEGACY_GAME_URL=http://localhost:5173/

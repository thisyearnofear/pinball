.DS_Store
node_modules
/dist
/backend/dist
/apps/web-react/dist/
/.next
/out
.pnpm-store/
*.tsbuildinfo
build.sh

# Lockfiles — pnpm's is committed, being the single source of truth for installs.
# npm's is ignored so a stray `npm install` cannot quietly introduce a second,
# divergent resolution of the same dependency tree.
*-lock.json

# Hardhat/Contracts
contracts/artifacts/
contracts/cache/
contracts/typechain-types/

# local env files
.env
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gemini
.qoder

# Local Netlify folder
.netlify
.blackboxcli/

# Command Code
.commandcode/

